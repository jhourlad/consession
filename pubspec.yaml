name: consession
description: A simple session package for Flutter that works on both mobile, web and (hopefully) desktop apps.
author: jhourlad<jhourlad01@gmail.com>
version: 0.0.6
homepage: https://github.com/jhourlad/consession

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^0.5.8
  flutter_session: ^0.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
